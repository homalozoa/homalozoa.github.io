{
  "name": "homalozoa-is-not-for-eating",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "海果不能吃 — Homalozoa X 的个人博客",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node --check scripts/build.mjs && node --check assets/site.js && python3 scripts/check.py"
  },
  "engines": {
    "node": ">=20"
  }
}
