{
  "name": "carboniferous",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Carboniferous — Homalozoa X 的技术笔记",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node --check scripts/build.mjs && node --check assets/site.js && python3 scripts/check.py"
  },
  "engines": {
    "node": ">=20"
  }
}
